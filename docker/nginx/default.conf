gzip on;
gzip_vary on;
gzip_min_length 1024;
gzip_types
  text/plain
  text/css
  text/javascript
  application/javascript
  application/json
  application/xml
  image/svg+xml;

etag on;
if_modified_since exact;
open_file_cache max=1000 inactive=30s;
open_file_cache_valid 60s;
open_file_cache_min_uses 2;
open_file_cache_errors on;
server_tokens off;
client_header_timeout 10s;
client_body_timeout 10s;
send_timeout 10s;
client_max_body_size 1m;
reset_timedout_connection on;

server {
  listen 80 default_server;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  charset utf-8;

  add_header Referrer-Policy "strict-origin-when-cross-origin" always;
  add_header X-Content-Type-Options "nosniff" always;
  add_header X-Frame-Options "SAMEORIGIN" always;

  location = /healthz {
    limit_except GET HEAD {
      deny all;
    }

    default_type text/plain;
    add_header Cache-Control "no-store" always;
    return 200 "ok\n";
  }

  location = / {
    return 301 /blog/;
  }

  location = /blog {
    return 301 /blog/;
  }

  location = /blog/runtime-config.js {
    limit_except GET HEAD {
      deny all;
    }

    alias /tmp/runtime-config.js;
    add_header Cache-Control "no-store" always;
  }

  location = /blog/index.html {
    limit_except GET HEAD {
      deny all;
    }

    add_header Cache-Control "no-store" always;
    try_files $uri =404;
  }

  location = /blog/posts/index.json {
    limit_except GET HEAD {
      deny all;
    }

    alias /srv/blog-posts/index.json;
    add_header Cache-Control "no-store" always;
  }

  location /blog/posts/ {
    limit_except GET HEAD {
      deny all;
    }

    alias /srv/blog-posts/;
    autoindex off;
    add_header Cache-Control "public, max-age=300, must-revalidate" always;
    access_log off;
  }

  location /blog/assets/ {
    limit_except GET HEAD {
      deny all;
    }

    add_header Cache-Control "public, max-age=31536000, immutable" always;
    access_log off;
    try_files $uri =404;
  }

  location ~* ^/blog/.+\.(?:avif|gif|ico|jpe?g|json|png|svg|txt|webp|woff2?)$ {
    limit_except GET HEAD {
      deny all;
    }

    add_header Cache-Control "public, max-age=3600" always;
    access_log off;
    try_files $uri =404;
  }

  location /blog/ {
    limit_except GET HEAD {
      deny all;
    }

    try_files $uri $uri/ /blog/index.html;
  }
}
